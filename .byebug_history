cur_place_id
Photo.find_photos_for_place(cur_place_id).count
Photo.find_photos_for_place(cur_place_id)
cur_place_id

















dress") 
link_uri = URI.parse(current_url).request_uri
expect(page.status_code).to eq(200)
page.click_link("Show", :match => :first)
expect(page).to have_link('Show')
expect(page).to have_content("Listing Places")
expect(page.status_code).to eq(200)  
visit(places_path)
page.click_link("Show", :match => :first)

















